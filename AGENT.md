# エージェント作業メモ

## 調査範囲

- まず依頼で指定されたファイルと、その直接の依存ファイルから確認する。
- `outputs/` は実験出力として扱い、依頼で具体的な出力ファイル名が指定されない限り列挙・読み込みしない。
- 生成済みのバイナリや共有ライブラリは、ソース調査の対象から外す。

## resctrl 開発

- MBA 制御は `ompt_resctrl/`、PMU 共通処理は `ompt_common/` に置く。
- resctrl/PMU など必須機能が使えない場合、代替動作へ自動で切り替えずに失敗させる。
- MBA は 7 段階の resctrl グループで制御する。
- C++ 変更後は、ローカルのツールチェーンが使える場合 `bash build_amr.sh` で検証する。
- `AMR_RESCTRL_MBA_STRICTNESS` は MBA の制限強度を調整する。既定値は `1.15`。

## 変更時の注意

- 編集前に `git status` を確認し、無関係な作業ツリー変更を壊さない。
- 実行時の環境変数や制御仕様は、実装近くまたは `README.md` に記録する。
